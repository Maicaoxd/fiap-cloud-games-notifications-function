apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: fiap-cloud-games
configMapGenerator:
  - name: rabbitmq-notifications-scripts
    files:
      - configure-rabbitmq.ps1
      - definitions.json
